import { BookingForm } from "@/components/forms/BookingForm";

/**
 * this page is used for the booking of appointment
 * @returns appointment page 
 */
export const BookAppointmentPage = () => {
  return <BookingForm />;
};
